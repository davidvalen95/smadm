import { KeepAsSrcPipe } from './keep-as-src.pipe';

describe('KeepAsSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new KeepAsSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
